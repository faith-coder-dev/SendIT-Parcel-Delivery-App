[phases.setup]
nixPkgs = ["python311", "gcc", "postgresql"]

[phases.install]
cmds = ["cd BACKEND && python -m venv .venv && ./.venv/bin/pip install -r requirements.txt"]

[start]
cmd = "cd BACKEND && ./.venv/bin/gunicorn app:app --bind 0.0.0.0:$PORT"
